__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Built Fusion API corpus. Autodesk's content, built per-user, never committed.
# Only the build OUTPUT is ignored. src/fusion_cad_mcp/corpus/ is the builder
# source package and must ship, so these patterns stay anchored or file-scoped.
/corpus/
corpus.jsonl
corpus.jsonl.tmp
frontier.json
frontier.json.tmp
scraper.log
src/fusion_cad_mcp/corpus/pages/

# Local backups from knowledge-file annotation passes
*.bak-*
